const roles = {
    student: 'student',
    teacher: 'teacher',
    admin: 'admin'
}

// enum Roles = {
    // Student = 'student',
    // Teacher = 'teacher',
    // Admin = 'admin'
// }

export default roles;